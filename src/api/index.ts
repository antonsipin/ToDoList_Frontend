import { updateTask, addTask, signUp, signIn, logout, resolveTask, deleteTask, getTasks } from './api'

export { 
    updateTask,
    addTask,
    signUp,
    signIn,
    logout,
    resolveTask,
    deleteTask,
    getTasks
}